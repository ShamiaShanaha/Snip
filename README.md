# Snip

Snip is a simple URL shortener built with Flask, PostgreSQL, SQLAlchemy, and Docker.

It takes a long URL and generates a short, shareable URL. Snip also tracks how many times each shortened URL has been visited.
